name: passive_crypto_income
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'  # FIXED: Kept original for stability

dependencies:
  flutter:
    sdk: flutter
  google_sign_in: ^6.2.1  # FIXED: Latest stable
  http: ^1.1.0  # As is
  web_socket_channel: ^3.0.3  # FIXED: Latest stable
  shared_preferences: ^2.2.2  # FIXED: Latest stable
  cupertino_icons: ^1.0.1  # As is
  provider: ^6.1.2  # FIXED: Added for state management

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0  # FIXED: Latest for stricter rules

flutter:
  uses-material-design: true
